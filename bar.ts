// In `foo.ts`, try to import round function from `mathjs` dependency by typing
// `round`. No auto-import will be suggested.
//
// Then uncomment the line below and go back to `foo.ts`. You'll be correctly
// suggested the auto-import.
// import { round } from "mathjs";
